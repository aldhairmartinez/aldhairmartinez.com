---
title: "This Website"
summary: "A static Next.js portfolio evolving, piece by piece, into an observed distributed application."
tags: ["Next.js", "TypeScript", "Grafana Faro", "Cloudflare Pages", "OpenTelemetry", "FastAPI", "Grafana Alloy"]
status: "live"
date: "2026-08-28"
githubUrl: "https://github.com/aldhairmartinez/aldhairmartinez.com"
featured: true
---

You're looking at it. This site started as a static Next.js portfolio, but the plan for it goes further: it's meant to become a working example of how a simple frontend evolves into an observed, full-stack system — without pretending to be more than it currently is at each step.

## What's here today

- Next.js (App Router) + TypeScript + Tailwind CSS, exported statically and served from Cloudflare Pages.
- Content (projects, writing) authored as MDX with typed frontmatter, so new entries don't require touching a component.
- Grafana Faro frontend instrumentation, live in both development and production — see [`/observability`](/observability) for the honest current-state breakdown.
- A FastAPI backend, instrumented with OpenTelemetry, running locally in Docker — **not yet hosted in production**. It handles the contact form, sending real email through Resend to `hello@aldhairmartinez.com`.
- Grafana Alloy, also running locally, relays the backend's traces over OTLP to Grafana Cloud Tempo / Application Observability.
- One continuous distributed trace from browser to backend: Faro propagates a W3C trace context header on the contact form's request, and FastAPI continues that same trace rather than starting a new one.

## Where it's going

The backend and Alloy are real and working — they're just not production infrastructure yet. What's still ahead:

```
FastAPI + Alloy (Docker Desktop, local)
  → AWS-hosted backend infrastructure
  → PostgreSQL (contact history, resume analytics, project analytics, deployment history)
  → Redis (once there's a real caching use case)
  → Kafka (once there's a real event-processing use case)
```

Google Workspace is also planned, to turn `hello@aldhairmartinez.com` from a forwarding address into a full mailbox. Kubernetes/EKS, Terraform, k6, and synthetic monitoring get introduced progressively as the backend's infrastructure footprint grows. The `/observability` page tracks what's implemented versus planned as that build-out happens.

## Why build it this way

The easy version of a portfolio site is a static page that says what I've done. The more useful version is a system that demonstrates it — where the README, the architecture, and the telemetry are all things a visitor (or another engineer) can actually inspect.
